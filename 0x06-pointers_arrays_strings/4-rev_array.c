#include "main.h"

/**
 * reverse array - Reverses the content of array  of the intiger.
 * @a: The array of the integer to be reserved
 * @n: The number of element iin the array.
 */

void reverse_array(int *a, int n)

{
	int tmp, index;

	for (index = n - 1; index >= n / 2; index--)
	{
	tmp 
	}	
